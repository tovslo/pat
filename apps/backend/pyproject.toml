[project]
name = "myapp-backend"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    # Web framework — ref: spec.md → FastAPI + Uvicorn
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.32.0",
    "python-multipart>=0.0.12",

    # Database — ref: spec.md → PostgreSQL, asyncpg, SQLAlchemy, Alembic
    "sqlalchemy[asyncio]>=2.0.0",
    "asyncpg>=0.30.0",
    "alembic>=1.14.0",

    # Task queue — ref: spec.md → RabbitMQ + Celery, rpc:// result backend
    "celery[librabbitmq]>=5.4.0",
    "kombu>=5.4.0",

    # Storage — ref: spec.md → S3-Compatible Storage, aioboto3
    "aioboto3>=13.0.0",

    # Validation — ref: spec.md → Pydantic
    "pydantic>=2.10.0",
    "pydantic-settings>=2.7.0",

    # Auth — ref: spec.md → JWT, HTTP-only cookies
    "python-jose[cryptography]>=3.3.0",
    "passlib[bcrypt]>=1.7.4",

    # HTTP client (for Mailgun API) — ref: spec.md → Mailgun
    "httpx>=0.28.0",

    # Observability — ref: spec.md → OpenTelemetry, Sentry
    "sentry-sdk[fastapi]>=2.19.0",
    "opentelemetry-sdk>=1.29.0",
    "opentelemetry-instrumentation-fastapi>=0.50b0",
    "prometheus-fastapi-instrumentator>=7.0.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0.0",
    "pytest-asyncio>=0.25.0",
    "pytest-cov>=6.0.0",
    "httpx>=0.28.0",
    "ruff>=0.9.0",
    "factory-boy>=3.3.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["app"]

[dependency-groups]
dev = [
    "pytest>=8.0.0",
    "pytest-asyncio>=0.25.0",
    "pytest-cov>=6.0.0",
    "ruff>=0.9.0",
    "factory-boy>=3.3.0",
]

[tool.uv]
python-preference = "only-managed"

[tool.ruff]
target-version = "py312"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM", "ASYNC"]
ignore = ["E501"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
